name: "display environment variables"

on:
  push:
  workflow_dispatch:

jobs:
  display:
    name: Display
    runs-on: ubuntu-latest
    steps:
      - name: Display environment variables
        run: env